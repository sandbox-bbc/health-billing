package com.linx.health.common

/**
 * Application-wide constants.
 */
object Constants {
    /**
     * Date format for API request/response (MM/DD/YYYY).
     * Used with @JsonFormat annotation.
     */
    const val DATE_FORMAT = "MM/dd/yyyy"
}
